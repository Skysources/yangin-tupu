import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";

// Firebase web ayarları herkese açıktır (tarayıcıya inen kodda görünür), gizli
// değildir. .env varsa oradan okunur; yoksa (ör. GitHub Actions derlemesi)
// aşağıdaki varsayılan değerler kullanılır.
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY || "AIzaSyARqFrQUONDufC6cdybGbTeNsRsyGhuAUg",
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN || "yangin-tupu.firebaseapp.com",
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID || "yangin-tupu",
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET || "yangin-tupu.firebasestorage.app",
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID || "743165469599",
  appId: import.meta.env.VITE_FIREBASE_APP_ID || "1:743165469599:web:0b185ef852b31e8716d3aa",
};

const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
